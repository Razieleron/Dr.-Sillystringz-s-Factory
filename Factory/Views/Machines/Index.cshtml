@{
  Layout = "_Layout";
}
@using Factory.Models;

<h2> Machine List </h2>

@if(Model.Count == 0)
{
  <h3>No Machine Added</h3>
}

@foreach (Machine item in Model)
{
  <p>@Html.ActionLink($"{item.MachineName}", "Details", new {id = item.MachineId})</p>
}