@{
  Layout = "_Layout";
}

@using Factory.Models;

<h2> Engineer List </h2>

@if(Model.Count == 0)
{
  <h3>No Engineer Added</h3>
}

@foreach (Engineer item in Model)
{
  <p>@Html.ActionLink($"{item.EngineerName}", "Details", new {id = item.EngineerId})</p>
}